import React from 'react';
import ReactDOM from 'react-dom/client';
import Activation from './pages/Activation';
import './pages/Activation.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Activation />
  </React.StrictMode>,
);
